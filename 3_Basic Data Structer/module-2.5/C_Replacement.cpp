#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n; cin >> n;
    vector <int> v(n);
    for(int i=0; i<n; i++)
    {
        int x; cin >>x;
        
        v[i] = x;
    }

    replace(v.begin(),v.

    for(int val:v)
    {
        cout << val <<" ";
    }
    return 0;
}