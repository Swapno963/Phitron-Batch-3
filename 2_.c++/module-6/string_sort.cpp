#include<bits/stdc++.h>
using namespace std;
int main()
{
    string s;
    cin >> s;
    sort(s.begin(),s.end());// it thake  first pointer and last pointer (/0)
    cout << s << endl;
    return 0;
}